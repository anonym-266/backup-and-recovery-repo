First  objet

const circle = {
    radius: 1,
    location: {
        x: 1,
        y: 1
    },
    draw: function() {
        console.log("draw");
    }
};

circle.draw();

enhanced with factory method

function createCircle (radius, xCoordinate, yCoordinate) {
    return {
        radius,
        location: {
            x: xCoordinate,
            y: yCoordinate
        },
        draw : function() {
            console.log("draw");
        }
    };
}

const circle = createCircle(3, 2, 1);

Second object


let baseSalary = 20000,
    overTime = 10,
    rate = 20;

function getWage(baseSalary, overTime, rate) {
    return baseSalary + (overTime * rate);
}
console.log(getWage(baseSalary,overTime, rate));

const employee = {
    baseSalary: 20000,
    overTime: 10,
    rate: 20,
    getWage: function() {
        return this.baseSalary + (this.overTime * this.rate);
    }
}

console.log(employee.getWage());

